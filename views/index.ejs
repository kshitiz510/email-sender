<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Send Email</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            height: 100vh;
            margin: 0;
            padding: 0;
        }

        .container {
            padding: 24px;
            border-radius: 16px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 500px;
        }

        h1 {
            text-align: center;
            color: #007BFF;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
        }

        label {
            margin-bottom: 8px;
            color: #333;
        }

        input,
        textarea {
            padding: 10px;
            margin-bottom: 14px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
            background-color: #f9f9f9;
            color: #333;
        }

        button {
            width: 25%;
            margin: auto;
            padding: 12px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 8px;
            font-weight: 700;
            cursor: pointer;
            transition: background-color 0.25s;
        }

        button:hover {
            background-color: #0058b5;
        }

        .link-container {
            display: flex;
            justify-content: space-between;
            align-items: last baseline;
            margin-top: 10px;
            padding: 0px 14px;
        }

        .link-container a {
            color: #007BFF;
            text-decoration: none;
            font-weight: 700;
        }

        .search-form {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 20px;
        }

        .search-form input {
            flex: 1;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 8px;
            font-size: 16px;
            background-color: #f9f9f9;
            color: #333;
            margin-right: 10px;
        }

        .search-form button {
            padding: 10px 20px;
            background-color: #007BFF;
            color: white;
            border: none;
            border-radius: 8px;
            font-weight: 700;
            cursor: pointer;
            transition: background-color 0.25s;
        }

        .search-form button:hover {
            background-color: #0058b5;
        }

    </style>
</head>


<body>
    <div class="container">
        <h1>Send Email</h1>
        <form action="/email" method="post">
            <label for="emails">Recipient Emails (comma-separated):</label>
            <input type="text" id="emails" name="emails" required>
            <br>
            <label for="subject">Subject:</label>
            <input type="text" id="subject" name="subject" required>
            <br>
            <label for="content">Content:</label>
            <textarea id="content" name="content" required></textarea>
            <br>
            <button type="submit">Send</button>
        </form>
        <div class="link-container">
            <a href="/email/sent" class="view-all">View All Emails</a>
            <div class="search-form">
                <input type="text" id="recipientEmail" placeholder="Enter Recipient Email">
                <a href="#" id="searchById">Search by Id</a>
            </div>
        </div>

    </div>

    <script>
        document.getElementById('searchById').addEventListener('click', function(event) {
            event.preventDefault()
            var recipientEmail = document.getElementById('recipientEmail').value
            if (recipientEmail) {
                window.location.href = '/email/sent/' + recipientEmail
            }
        })
    </script>

</body>

</html>